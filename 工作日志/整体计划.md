# MyAgent 渐进式开发路线图

## 目标

搭建一个**个人知识库问答智能体**，具备 RAG（检索增强生成）、工具调用、对话记忆、**多智能体协作**四大核心能力。

## 策略

先用 LangChain/LangGraph 的高层封装快速跑通，再逐步深入底层实现。每个阶段都产出一个**可运行的版本**。

## 技术栈

- LLM: ChatTongyi (DeepSeek V3.1 via DashScope)
- 框架: LangChain + LangGraph
- 语言: Python

## 节奏

每天 3-4 小时，每个 Phase 预计 1-3 天。

---

## 路线总览

```
Phase 1 (Day 1)      基础 Agent        ← 用预构建函数跑通工具调用              ✅ 已完成
Phase 2 (Day 2-3)    加入 RAG          ← 能基于本地文档回答问题                ✅ 已完成
Phase 3 (Day 4-5)    加入 Memory       ← 对话记忆持久化，跨会话记住上下文        ✅ 已完成
Phase 4 (Day 6-8)    深入 Agent 图     ← 替换预构建为手动 LangGraph             ✅ 已完成
Phase 5 (Day 9-11)   深入 RAG 管线     ← 自定义分块、混合检索、查询改写          ✅ 已完成
Phase 6 (Day 12-14)  高级特性          ← 流式输出、Web UI、长期记忆             ✅ 已完成 (V1.0)
Phase 7 (Day 15-17)  多智能体协作      ← Supervisor + Workers 架构             ✅ 已完成 (V2.0)
Phase 8 (Day 18+)    协作增强          ← 并行执行、任务依赖、人工介入            🚧 待开发
Phase 9 (Day 20+)    高级多智能体      ← 动态 Agent、分布式执行                 🚧 待开发
```

---

## Phase 1 — 基础 Agent ✅

**目标**: 用 `create_react_agent` 预构建函数，最快速度跑出一个能调工具的 Agent。

**产出**: CLI 交互式 Agent，能调用 3 个工具回答问题。

**内容**:
- 项目基础设施: `.gitignore`、`.env`、`requirements.txt`
- agent 包: `config.py`（环境变量）、`llm.py`（LLM 封装）、`tools.py`（3 个工具）
- 3 个工具: 日期时间、安全计算器、DuckDuckGo 搜索
- CLI 入口: `main.py`，使用 `create_react_agent` + 交互式聊天循环

---

## Phase 2 — 加入 RAG ✅

**目标**: 能加载本地文档（PDF/Markdown/TXT），基于文档内容回答问题。

**内容**:
- 新增依赖: `faiss-cpu`、`langchain-text-splitters`、DashScope embedding
- 创建 `agent/rag/` 子包: `loader.py`、`vectorstore.py`、`retriever.py`
- 将 Retriever 包装为 Agent 工具
- 创建 `scripts/index_docs.py` 一键索引文档

---

## Phase 3 — 加入 Memory ✅

**目标**: 对话记忆持久化，关闭程序后重开仍然记住之前的对话。

**内容**:
- `agent/memory/checkpointer.py` — SqliteSaver 配置
- `thread_id` 管理 — 支持 `/thread <id>` 切换、`clear` 新建
- 消息窗口限制（MAX_MESSAGES=10）

---

## Phase 4 — 深入 Agent 图 ✅

**目标**: 将 `create_agent` 替换为手动构建的 LangGraph 状态图。

**内容**:
- `agent/graph.py` — 手动定义 `AgentState`、`StateGraph`
- 6 个节点: `trim`、`rewrite`、`agent`、`tools`、`increment`、`force_reply`
- 查询改写节点、工具调用上限保护

---

## Phase 5 — 深入 RAG 管线 ✅

**目标**: 替换 Phase 2 的高层封装，自定义每个 RAG 环节。

**内容**:
- Markdown 语义分块（MarkdownHeaderTextSplitter）
- 混合检索（FAISS + BM25 + RRF 融合）
- 查询改写增强

---

## Phase 6 — 高级特性 ✅ (V1.0 完结)

**目标**: 让单智能体具备完整的产品形态。

**内容**:
- 流式输出（`astream_events`）
- Web UI（Gradio）
- 长期记忆（跨会话用户画像）

---

## Phase 7 — 多智能体协作 ✅ (V2.0)

**目标**: 从单一智能体升级为多智能体协作系统。

**架构**: Supervisor + Workers

```
用户输入 → Supervisor (任务分解)
              │
              ├─→ Code Agent ─┐
              ├─→ Data Agent ─┼─→ Aggregator → 最终输出
              └─→ Writer Agent┘
```

**内容**:
- `agent/multi/` 多智能体模块
- `MultiAgentState` 共享状态
- Supervisor 路由决策 + Aggregator 结果汇总
- 三个专业 Worker: Code / Data / Writer
- CLI `--multi` 参数、WebUI 模式切换

---

## Phase 8 — 协作增强（待开发）

**目标**: 让多智能体系统更智能、更高效。

| 特性 | 说明 |
|------|------|
| 并行执行 | 独立子任务并行处理 |
| 任务依赖分析 | 自动识别任务间的依赖关系 |
| Agent 间消息传递 | 更丰富的上下文交接 |
| Human-in-the-loop | 关键节点人工审核 |

---

## Phase 9 — 高级多智能体（待开发）

**目标**: 生产级多智能体系统。

| 特性 | 说明 |
|------|------|
| 动态 Agent 生成 | 根据任务自动创建专业 Agent |
| Agent 评估与反馈 | 自动评估 Agent 表现并优化 |
| 流式输出支持 | 多智能体模式的流式响应 |
| 分布式执行 | 支持多机部署 |
